---
const footerConfig = {
  sections: [
    {
      title: 'Legal',
      links: [
        {
          label: 'Terms',
          url: 'https://rsvp.kim/privacy-terms#terms',
        },
        {
          label: 'Privacy Policy',
          url: 'https://rsvp.kim/privacy-terms',
        },
      ],
    },
    {
      title: 'Connect',
      socialLinks: [
        {
          platform: 'Email',
          url: 'mailto:support@rsvp.kim',
          icon: 'envelope',
        },
        {
          platform: 'Twitter',
          url: 'https://x.com/TeamShiksha',
          icon: 'twitter',
        },
      ],
    },
    {
      title: 'Powered by Team.Shiksha',
      tag: '© 2025 Team Shiksha. All rights reserved.',
    },
  ],
};
---

<footer class='py-12'>
  <div class='max-w-7xl mx-auto px-4 sm:px-6 lg:px-8'>
    <div class='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8'>
      <div>
        <a href='/' class='text-xl font-bold'>Help Center</a>
      </div>
      {
        footerConfig.sections.map((section) => (
          <div>
            <h3 class='font-semibold mb-4'>{section.title}</h3>
            {section.tag && (
              <p class='text-sm text-gray-500 mb-4'>{section.tag}</p>
            )}
            {section.links && (
              <ul class='space-y-2'>
                {section.links.map((link) => (
                  <li>
                    <a href={link.url} target='_blank'>
                      {link.label}
                    </a>
                  </li>
                ))}
              </ul>
            )}
            {section.socialLinks && (
              <div class='flex flex-col space-y-2 '>
                {section.socialLinks.map((social) => (
                  <a
                    href={social.url}
                    aria-label={social.platform}
                    target='_blank'
                  >
                    {/* <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 24 24">
                    <!-- Replace this with a real icon system if needed -->
                    <title>{social.platform}</title>
                    <path d="M0 0h24v24H0z" fill="none"></path>
                    <path d={social.icon}></path>
                  </svg> */}
                    <span>{social.platform}</span>
                  </a>
                ))}
              </div>
            )}
          </div>
        ))
      }
    </div>
  </div>
</footer>
